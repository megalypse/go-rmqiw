{
  "name": "Capture payment",
  "description": "Publishes a mock payment capture event and waits until the backend records it.",
  "steps": [
    {
      "name": "Publish payment.captured",
      "description": "Sends a payment.captured message to the mock backend.",
      "poll_interval": 1000000000,
      "poll_query": "SELECT EXISTS (SELECT 1 FROM mock_events WHERE flow_step = 'capture-payment')",
      "message": {
        "exchange": "rmqiw.mock",
        "routing_key": "payments.captured",
        "headers": {
          "flow_step": "capture-payment"
        },
        "body": "{\"id\":\"payment-1\",\"order_id\":\"order-1\",\"amount\":42.50}"
      }
    }
  ]
}
